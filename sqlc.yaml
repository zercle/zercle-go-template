version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/migration"
    queries: "sql/query"
    gen:
      go:
        sql_package: "pgx/v5"
        out: "infrastructure/sqlc/db"
        emit_interface: true
        emit_json_tags: true
